# CMAKE_CONFIG_POSTFIX

参考文档

* [CMAKE_CONFIG_POSTFIX](https://cmake.org/cmake/help/latest/variable/CMAKE_CONFIG_POSTFIX.html)

对于配置`CONFIG`,库文件默认的后缀.

当一个不是可执行文件的目标类型被创建后，它的目标属性`CONFIG_POSTFIX`就会被初始化为当前的`CMAKE_CONFIG_POSTFIX`变量的值.
